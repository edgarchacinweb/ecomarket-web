---

---

<header class="header" id="home">
    <h1 class="header-title">Your online store instantly</h1>
    <h2 class="header-subtitle">
        Build an online store without worrying about the server. Easy, fast and
        secure
    </h2>
    <a
        href="https://github.com/edgarchacinweb/ecomarket-web"
        target="_blank"
        class="header-cta"
    >
        <img src="github.svg" alt="github icon" />
        View code on github
    </a>
</header>

<style>
    .header {
        width: 100%;
        height: 100vh;
        position: relative;
        background-image: url("header.jpg");
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        gap: 25px;
        color: #ffffff;
    }

    .header::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background: rgba(0, 0, 0, 0.4);
    }

    .header-title {
        z-index: 100;
        font-size: 2rem;
        margin-top: 6rem;
    }

    .header-subtitle {
        z-index: 100;
        font-size: 1rem;
        text-align: center;
        padding: 0 2rem 2rem 2rem;
    }

    .header-cta {
        position: relative;
        z-index: 100;
        width: 60vw;
        min-width: 20rem;
        max-width: 30rem;
        font-size: 1.3rem;
        text-align: center;
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        padding: 1rem;
        border-radius: 0.6rem;
        overflow: hidden;
        border: 2px solid rgba(250, 250, 250, 0.7);
        color: #ffffff;
        text-decoration: none;
        font-weight: bold;
        transition: all 350ms ease;
    }

    .header-cta:hover {
        box-shadow: 0 0 10px 2px #ffffff;
    }

    .header-cta::before {
        content: "";
        z-index: -1;
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        background-color: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(5px);
    }

    .header-cta img {
        width: 50px;
        height: 50px;
        filter: invert(100%);
    }
</style>
